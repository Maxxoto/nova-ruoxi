[project]
name = "nova-ruoxi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "langchain-core>=0.3.68",
    "langchain-groq>=0.3.5",
    "langgraph>=0.5.1",
    "streamlit>=1.46.1",
    "watchdog>=6.0.0",
    "langfuse",
    "langchain>=0.3.26",
]
[tool.ruff]
line-length = 200
exclude = [".venv"]
